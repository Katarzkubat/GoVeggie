package com.example.katarzkubat.goveggie.ViewModel;

/*

import android.arch.lifecycle.LiveData;
import android.arch.lifecycle.ViewModel;

import com.example.katarzkubat.goveggie.Model.Favorites;
import com.example.katarzkubat.goveggie.Database.AppDatabase;

public class AddToFavoritesViewModel extends ViewModel {

    private LiveData<Favorites> favoritesLiveData;

    public AddToFavoritesViewModel(AppDatabase mDb, int favoriteId) {
        favoritesLiveData = mDb.favoriteDao().loadFavoritesById(favoriteId);
    }

    public LiveData<Favorites> getFavoritesLiveData() {
        return favoritesLiveData;
    }
} */
