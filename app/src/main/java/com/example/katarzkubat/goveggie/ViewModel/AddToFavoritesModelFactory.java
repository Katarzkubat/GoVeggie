package com.example.katarzkubat.goveggie.ViewModel;
/*
import android.arch.lifecycle.ViewModel;
import android.arch.lifecycle.ViewModelProvider;
import android.support.annotation.NonNull;

import com.example.katarzkubat.goveggie.Database.AppDatabase;

public class AddToFavoritesModelFactory extends ViewModelProvider.NewInstanceFactory {

    private final AppDatabase mDb;
    private final int row_id;

    public AddToFavoritesModelFactory(AppDatabase mDb, int row_id) {
        this.mDb = mDb;
        this.row_id = row_id;
    }

    @NonNull
    @Override
    public <T extends ViewModel> T create(@NonNull Class<T> modelClass) {
        return (T) new AddToFavoritesViewModel(mDb, row_id);
    }
} */
